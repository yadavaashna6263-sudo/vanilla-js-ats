This folder is intentionally empty.

Spotlight uses inline SVG for every icon and illustration in the markup, so the app ships with zero external image dependencies. Drop real photography, icon sets, or illustrations here if you want to extend it — just update the relevant HTML/CSS to point at them.
